import { defineSystem } from "bitecs";
import type { IWorld } from "bitecs";

export default function UFOSpawnSystem()
{
  return defineSystem( ( _world : IWorld ) => {



    return _world;
  } );
}
